<h1>List - 유상증자</h1>
<p> 유상증자가 예정들인 회사 리스트입니다. 권리락일까지 보유한 주식수에 신주배정비율을 곱합니다.</p>
<p> 나온 값과 기존 보유한 주식수를 더합니다. </p>
<p> 신주 배정일 전까지, 상기 값을 반영하되, 새로 추가된 신규 주식들은 가상 주식임을 표합니다. 신주 배정예정일날, 가상 주식은 실제 주식이 됩니다.</p>

<table>
  <thead>
    <tr class="main_table">
      <th>회사명</th>
      <th>주식코드</th>
      <th>권리락일</th>
      <th>신주배정비율</th>
      <th>현재 보유주식수</th>
      <th>신규 주식수</th>
      <th colspan="5"></th>
    </tr>
  </thead>

  <tbody>
    <% @company_stocks.each do |company_stock| %>
      <tr class="<%= cycle('list_line_odd', 'list_line_even') %>">
        <td class="name"><%= company_stock.name %></td>
        <td class="sharecode"><%= company_stock.sharecode %></td>
        <td><%= company_stock.EndDate %></td>
        <td><%= company_stock.new_stock_issuance_ratio %></td>
        <td><%= company_stock.current_stock_amount %></td>
        <td><%= company_stock.NewTotalStocks %></td>
        <td><%= button_to 'B.Bridge 보내기'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '회사 검색', new_company_stock_path %>



